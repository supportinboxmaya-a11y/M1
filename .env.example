# Core Maya URL — where M1 connects to health-ping
CORE_MAYA_URL=http://localhost:8000

# M1's own HTTP port
M1_PORT=3001

# Gate — set to true to activate M1
M1_ENABLED=false

# Ping interval in milliseconds (default 30s)
M1_PING_INTERVAL_MS=30000
